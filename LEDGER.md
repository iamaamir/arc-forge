# Ledger

Single home for defects, rulings, and non-goals across all arc-forge work. Split-brain is forbidden: do not track findings in changelogs, session notes, or scratch files — everything lands here with an evidence link.

**Closure rule:** a fix batch's commit message MUST reference the ledger IDs it closes. Reviewers verify closure against this file, not against changelogs.

## DEFECTS

| ID | Severity | Status | Finding | Evidence |
|----|----------|--------|---------|----------|
| D1 (round-2: F6) | Medium / Minor | OPEN | SetupError messages surfaced through `result.message` print without the promised `forge-gate: ` prefix; prefix exists only on flag-validation and crash paths (`bin/forge-gate.mjs` `executeGates` vs `reportGateCrash`). Machine parsers keying on the prefix miss most setup errors. | qa-lab/phase3-report.md (D1); axiom-lab/findings.md F6; verified by execution in axiom-lab/audit.md §1 |
| D2 (round-2: F1) | High / Important | OPEN | On timeout, the timed-out branch discards the child's drained partial output (`src/check.mjs` timedOut branch never includes `result.output`), contradicting design comments in `commands.mjs`. Agents lose the only diagnostics a hanging suite produced. | qa-lab/phase3-report.md (D2); axiom-lab/findings.md F1; reproduced in axiom-lab/audit.md §1 (marker absent) |
| D3 (round-2: F9) | Medium / Minor | OPEN | Null mutation report crashes raw: `TypeError: Cannot read properties of null (reading 'files')` (`mutation.mjs`). Schema drift should be a named setup error naming path + regenerate command. | qa-lab/phase3-report.md (D3); axiom-lab/findings.md F9; reproduced in axiom-lab/audit.md §1 |
| D4 (round-2: F3) | Low / Important | OPEN | Bare `check` hard-requires `qaCommand`: green spec+crap+mutation project without it gets exit 2; deps gate skips-with-notice — asymmetry widened, and README §2 scaffold (`qaCommand: ""`) guarantees the red. Explicit flag must still enforce. | qa-lab/phase3-report.md (D4); axiom-lab/findings.md F3; reproduced in axiom-lab/audit.md §1 incl. README contradiction |
| F2 | Important | OPEN | Truncation notice appended AFTER up to ~10 MB of echoed partial output: capped consumers (default spawnSync maxBuffer 1 MB) get ENOBUFS/SIGTERM and **no exit code at all** — breaking "agents cannot argue with an exit code" in exactly the chatty-failure scenario targeted. Put notice first, cap echoed bytes head/tail. | axiom-lab/findings.md F2; exactly reproduced in axiom-lab/audit.md §1 (ENOBUFS, status null) |
| F5 | Important | OPEN | `"dependencyRules": null` defeats the bare-check skip (`!== undefined`) and crashes raw via `assertRulesObject` accepting null (`typeof null === "object"`). New skip logic routes a latent validation hole into a first-run crash path. Reject null explicitly; make skip guard truthiness-based. | axiom-lab/findings.md F5; reproduced in axiom-lab/audit.md §1 |
| M1 | Important | OPEN | Q4 ruling unimplemented in the dangerous direction: `commandTimeoutSeconds: 0` silently disables timeout protection (`spawnSync(timeout: 0)` treats 0 as absent → hanging suite hangs forever). Negatives exit 2, so "≤ 0 rejected" is half-true. Reject or treat-as-unset per final Q4 ruling. | axiom-lab/audit.md §2 (M1), executed probe; phase3 C6.08 owed gap |
| M2 | Important | OPEN | Q1(a) config-file threshold type-validation still absent: `crapThreshold: "abc"` exits 1 (garbage comparison coerces) instead of named SetupError. CLI-flag numeric validation was fixed; config-file path unvalidated — internal asymmetry of the D4 kind. | axiom-lab/audit.md §2 (M2), executed probe; phase3 known-deviations Q1(a) C13.07/.08 |
| F4/Q3 | Important | OPEN | Adjudicated help-masks-misuse behavior not implemented: any help token anywhere wins over misuse, exits 0 (`bin/forge-gate.mjs` help short-circuit precedes flag validation); charter fallback option (b)'s stderr warning also absent; help text now codifies the deviant behavior. Scan args first; typo'd invocations must exit 2. | axiom-lab/findings.md F4; adjudication in qa-lab/charters-review.md §E (Q3); reproduced in axiom-lab/audit.md §1 |

## RULINGS

Employer rulings from qa-lab/notes/questions-to-human.md (Q1–Q8) with charters-review §E adjudications. Status reflects implementation as of this ledger's creation.

| ID | Question | Ruling (verbatim) | Source | Status |
|----|----------|-------------------|--------|--------|
| Q1 | Should config-value type errors be exit 2 (setup) instead of exit 1? | "**Proceeded with:** charter C13.3 asserts exit 2 as required behavior; current behavior logged as finding F1." Charters review: "**Endorse (a)** — Conflating 'your code is bad' with 'your config is broken' is precisely the confusion this product sells against; (c) is disqualifying." | questions-to-human.md:12; charters-review.md:153 | PARTIAL — CLI flags validated (phase3 C4.24x PASS); config-file path still open → defect M2 |
| Q2 | Decorator error wording vs README promise? | "**Proceeded with:** charter C2e asserts the README promise; logged as F4." Charters review: "**Endorse (a)** — One-line change keeps the README as enforceable contract." | questions-to-human.md:19; charters-review.md:154 | FIXED — line-initial decorators now name the syntax (phase3 C2.05 PASS); inline-decorator wording gap folded into follow-up |
| Q3 | Help token short-circuits misuse reporting? | Charters review overrides rec (b): "**Better option:** honor `-h` only when no unknown flags precede it — An agent scripting a typo'd invocation receiving exit 0 falsifies 'agents cannot argue with an exit code'; if (b) is chosen anyway, print the suppressed misuse warning on stderr beneath the help." Charter C5.9 pins whichever is declared intentional. | questions-to-human.md:27; charters-review.md:155 | OPEN — unimplemented → defect F4/Q3 |
| Q4 | `commandTimeoutSeconds: 0` semantics? | Employer rec: "(a) — mirrors the `--roots=` empty-means-unset asymmetry precedent". Charters review: "**(b)** — reject ≤0 with exit 2 — giving 0 a third meaning ('unset') contradicts the strictness posture." Charter C6.7/C10.5. | questions-to-human.md:32; charters-review.md:156 | OPEN — ruling disputed/unimplemented; 0 currently accepted → defect M1 |
| Q5 | Whitespace-only testCommand passes? | "Recommend trimming + rejecting blank commands at config load (consistent with empty-string handling which already exits 2). Charters review: **Endorse** — A whitespace no-op passing `--spec` manufactures an unearned green." Charter C7.5; logged F5 (round 1). | questions-to-human.md:38; charters-review.md:157 | OPEN — phase3 C7.04 known-issue |
| Q6 | `roots` entry that is a file, not a directory? | Charters review strengthens to: "**Strengthen to error (exit 2)** — Roots are directories by contract… stderr warnings are invisible to exit-code-scraping agents — the exact audience." Charter C7.7; logged F3 (round 1). | questions-to-human.md:43; charters-review.md:158 | OPEN — phase3 C7.06 file-as-root silently green |
| Q7 | No upward search for forge-gate.config.json? | "**Endorse**, with rider — Hint must state 'no config file found — defaults in effect', go to stderr only, and never alter the exit code; do NOT walk up to git root." Charter C14.3/5. | questions-to-human.md:48; charters-review.md:159 | OPEN — phase3 C7.09 hint absent, exit unaffected |
| Q8 | Scope of phase-2 automation (offline/orphan reaping)? | "**Endorse the proxy approach**; record orphan-reaping under stage-5 exception if needed — Proxy-env simulation is deterministic and portable… note C10.2's marker-file assertion already covers the kill-tree property without process-tree inspection tooling." Deferred decision to phase-2 planning. | questions-to-human.md:54; charters-review.md:160 | PARTIAL — offline automation deferred to phase 2; orphan kill-gap confirmed (phase3 C10.02) |

## NON-GOALS

Consciously accepted deviations. Each has a one-line rationale and a permanent home. Never silently dropped.

| Non-goal | Rationale | Documented |
|----------|-----------|------------|
| `.tsx` / JSX parsing deferred | Clean SetupError names the limitation pending parser decision; guard fires only when scanner encounters such files. | spec Deferrals (2026-08-26-forge-gate-v2-design.md:26,136); forge-gate README:165 |
| Junior-dev training mode deferred | Human-process design (gates without AI crutch); revisit after gates stabilize — nothing in current design blocks it. | spec:135; forge-gate README:236 ("Deferred, not forgotten") |
| UML drill-down viewer cut from v2 | Static dependency graph slice only; interactive drill-down formally out of scope. | gauntlet-state.md Decisions (S1–S4); spec Amendments; docs/superpowers visualization.md intro |
| Non-literal dynamic imports skipped | `import(someVariable)` not resolvable statically; documented non-goal. | spec:113 |
| Prose QA documents ephemeral; executable `qaScript` persists | The script is test code and the lasting artifact; prose exists only to be converted into it (stage-5 gate enforces executability). | forge-gate README:234; gauntlet-state.md (C1 decision); stage-1-specify.md:15; stage-5-qa.md:13 |
| CRAP threshold default 8 (= top of transcript agent range 6–8 vs human 4) | Agents get the widened end of the source workflow's range; raising further requires explicit user approval. | stage-3-clean.md:11; AGENTS.md summary (orientation only) |
| Mutation score default 95, not 100 | Provably equivalent mutants genuinely exist at this strength (87 documented equivalents at 92.32%); below 95 means tests too weak, above becomes kill-or-prove-everything. | stage-4-harden.md:9; gauntlet-state.md (C2 decision) |
| Matcher-cache deletion accepted as performance-only (F7) | Killed mutants honestly by deleting memoization; measured cost (~38× compile overhead, 113→54 ms in-situ) owed to this ledger per round-2 ruling — re-add allowed only with direct tests pinning memoization semantics. | gauntlet-state.md dead-code list; axiom-lab/audit.md §1 (F7 benchmark); partially recorded — benchmark number now here |
| `classifyInstalledPackage` fails loudly on racy dangling node_modules entry | Raw TypeError chosen over silent misclassification; exit contract holds. Round-2 F10(b): either raise named SetupError or record as non-goal — recorded here. | gauntlet-state.md dead-code list; deps.mjs comments |
| Lesson-craft reference files have no test harness | Browser DOM components; no jsdom/happy-dom dep available. Refactors verified by inspection + syntax check only. | gauntlet-state.md documented-risk entry |
| one-thing-function arg-count advisory only | CRAP ≤ 8 is the enforced gate; remaining multi-arg helpers are pre-existing repo style. | gauntlet-state.md arg-count audit entry |
| Nesting depth > ~400 levels unrepresentable on macOS (PATH_MAX) | Environment bound, not a product claim; deeper trees cannot be fixture-built on this platform. | qa-lab/phase3-report.md friction #4 |
| amaro `engines: node >=22` documented but not enforced | Verified working on Node 20.20.2 (WASM); enforcement would block valid installs. | gauntlet-state.md improvement-findings |
| Specs ephemeral (`.feature` files excepted) | Gherkin specs persist as executable acceptance criteria kept green by `--spec`; big upfront design docs rejected. | SKILL.md rule 4; forge-gate README:234 |
